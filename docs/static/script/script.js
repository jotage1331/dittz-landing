/**
 * Dittz Climatização & Elétrica — script.js (global)
 *
 * Espaço reservado para utilitários JavaScript compartilhados entre páginas.
 * Lógica específica de cada módulo fica nos próprios templates via {% block scripts_extra %}.
 */

// Sobrescreve confirm padrão com algo mais elegante no futuro (placeholder)
// window.confirmar = (msg) => window.confirm(msg);
